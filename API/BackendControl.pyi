from pathlib import Path


class Backend:
    """
    Класс для динамической загрузки модуля на основе окружения.

    Ищет и загружает модуль по шаблону:
    _<name>_<платформа>[_<менеджер_окон>].py в указанном пути.

    Для Linux добавляет информацию о менеджере окон (из XDG_SESSION_TYPE),
    для других ОС этот параметр не используется.

    После загрузки все атрибуты модуля становятся атрибутами экземпляра этого класса.
    """
    
    def __init__(self, path: Path, name: str) -> None:
        """
        Инициализирует Backend, загружая соответствующий модуль.

        Args:
            path: Путь к директории с модулями
            name: Базовое имя модуля для поиска (используется в шаблоне имени файла)

        Загруженный модуль определяется по шаблону:
        _<name>_<sys.platform>[_<XDG_SESSION_TYPE>].py

        После загрузки все атрибуты модуля копируются в экземпляр Backend.
        """
        ...